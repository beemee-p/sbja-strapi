'use strict';

/**
 * portfolio-video controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::portfolio-video.portfolio-video');
